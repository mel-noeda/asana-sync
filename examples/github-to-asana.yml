# Copy to .github/workflows/github-to-asana.yml in the consuming repository.
# Requires the secrets listed in README.md.
#
# GitHub Actions cannot trigger on projects_v2_item. The two-minute schedule
# reconciles board column moves. For faster delivery, forward an org/user
# webhook as repository_dispatch type github_project_item.

name: GitHub → Asana

on:
  issues:
    types:
      - opened
      - edited
      - closed
      - reopened
      - labeled
      - unlabeled
      - assigned
      - unassigned
  pull_request:
    types:
      - opened
      - edited
      - closed
      - reopened
      - labeled
      - unlabeled
      - synchronize
      - ready_for_review
      - converted_to_draft
  schedule:
    - cron: "*/2 * * * *"
  repository_dispatch:
    types: [github_project_item]
  workflow_dispatch:
    inputs:
      issue_number:
        description: "GitHub issue or PR number (leave empty to reconcile all project columns)"
        required: false
        type: string
      columns_only:
        description: "Only sync Status → Asana section"
        required: false
        type: boolean
        default: false
      sync_all_project_items:
        description: "Sync every Issue/PR on the Projects v2 board"
        required: false
        type: boolean
        default: false

permissions:
  contents: read
  issues: read
  pull-requests: read

concurrency:
  group: g2a-${{ github.repository }}-${{ github.event.issue.number || github.event.pull_request.number || github.event.client_payload.issue_number || inputs.issue_number || 'columns' }}
  cancel-in-progress: true

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Resolve sync mode
        id: mode
        env:
          EVENT_NAME: ${{ github.event_name }}
          ISSUE_FROM_EVENT: ${{ github.event.issue.number || github.event.pull_request.number || github.event.client_payload.issue_number || inputs.issue_number }}
          DISPATCH_REPO: ${{ github.event.client_payload.github_repo }}
          DISPATCH_STATUS: ${{ github.event.client_payload.status }}
          INPUT_SYNC_ALL: ${{ inputs.sync_all_project_items }}
          INPUT_COLUMNS_ONLY: ${{ inputs.columns_only }}
        run: |
          columns_only=false
          sync_all=false

          if [ "$EVENT_NAME" = "schedule" ]; then
            sync_all=true
            columns_only=true
          elif [ "$EVENT_NAME" = "repository_dispatch" ]; then
            columns_only=true
          elif [ "$EVENT_NAME" = "workflow_dispatch" ]; then
            if [ "$INPUT_SYNC_ALL" = "true" ] || [ -z "$ISSUE_FROM_EVENT" ]; then
              sync_all=true
            fi
            if [ "$INPUT_COLUMNS_ONLY" = "true" ]; then
              columns_only=true
            fi
          fi

          echo "issue_number=${ISSUE_FROM_EVENT}" >> "$GITHUB_OUTPUT"
          echo "sync_all=${sync_all}" >> "$GITHUB_OUTPUT"
          echo "columns_only=${columns_only}" >> "$GITHUB_OUTPUT"
          echo "dispatch_repo=${DISPATCH_REPO}" >> "$GITHUB_OUTPUT"
          echo "dispatch_status=${DISPATCH_STATUS}" >> "$GITHUB_OUTPUT"

      - uses: mel-noeda/asana-sync@v1
        with:
          mode: g2a
          asana_token: ${{ secrets.ASANA_TOKEN }}
          github_token: ${{ secrets.ASANA_SYNC_GITHUB_TOKEN }}
          asana_project_gid: ${{ secrets.ASANA_PROJECT_GID }}
          github_project_owner: ${{ secrets.GITHUB_PROJECT_OWNER }}
          github_project_number: ${{ secrets.GITHUB_PROJECT_NUMBER }}
          github_repo: ${{ steps.mode.outputs.dispatch_repo || secrets.GITHUB_REPO || github.repository }}
          issue_number: ${{ steps.mode.outputs.issue_number }}
          columns_only: ${{ steps.mode.outputs.columns_only }}
          sync_all_project_items: ${{ steps.mode.outputs.sync_all }}
          github_project_status: ${{ steps.mode.outputs.dispatch_status }}
